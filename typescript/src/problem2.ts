/**
 * Write a factorial function.
 * The function should take a single positive integer and return a single integer value representing its factorial.
 *
 * To test your solution, run `yarn test2` in the console.
 *
 * @param {number} value - The number to calculate the factorial of. You may assume that value is greater than or equal to 1.
 * @returns {number} The factorial of the given number.
 * @example factorial(5) // => 120
 */
export default function factorial(value: number): number {
	// Your code goes below
	throw new Error('not implemented');
}
