/**
 * Write a function that takes a number as a parameter.
 * The function should sum each digit of the number and determine if the resulting sum is a palindrome.
 * NOTE: A palindrome is a word or number that stays the same when reversed (e.g. 121 or 5225)
 *
 * To test your solution, run `yarn test5` in the console.
 *
 * @param {number} num The number to check. You may assume a positive number.
 * @returns {boolean} True if the number is a palindrome, false otherwise.
 * @example isDigitSumPalindrome(56);  // true  (digit sum is 11)
 * @example isDigitSumPalindrome(296); // false (digit sum is 17)
 */
export default function isDigitSumPalindrome(num) {
	// Your code goes below
	throw new Error('not implemented');
}
