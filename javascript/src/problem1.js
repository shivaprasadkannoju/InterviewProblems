/**
 * Write a function that returns the length of the longest string in an array.
 * It should accept an array of string values as a parameter and return a single
 * integer representing the longest string.
 *
 * To test your solution, run `yarn test1` in the console.
 *
 * @param {string[]} values Array of string values to find the longest string in. You may assume that the array will not be null or empty.
 * @returns {number} The length of the longest string in the array.
 * @example longestStr(["the", "quick", "brown", "fox", "jumped", "over", "the", "lazy", "dog"]); // 6
 */
export default function longestStr(values) {
	// Your code goes below.
	throw new Error('not implemented');
}
