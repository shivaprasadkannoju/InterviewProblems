/**
 * Given a collection of integers, create a new collection containing the difference between each consecutive element in the collection.
 *
 * To test your solution, run `yarn test4` in the console.
 *
 *  @param {number[]} arr A collection of integers.
 * You will find the difference between each consecutive element in this collection.
 * You may assume that this array is not empty and will contain at least two elements.
 * @returns {number[]} A new collection of integers that represents the difference between each consecutive element in the input collection.
 * @example consecutiveDifference([4, 3, 6, 8, 2]); // [1, -3, -2, 6]
 */
export default function consecutiveDistance(arr) {
	// Your code goes below
	throw new Error('not implemented');
}
